{
  "schema_version": 1,
  "phase": "prebootstrap",
  "cap_gb": 18,
  "warning_percent": 75,
  "hard_stop_percent": 85,
  "additional_roots": [
    {
      "label": "local_attributable",
      "path": "%LOCALAPPDATA%\\ThriveLens",
      "purpose": "Attributable SDKs, bounded caches, task worktrees, portable PostgreSQL data, and generated build evidence outside the synced path",
      "required_in_phases": ["bootstrap_active", "implementation", "release"]
    }
  ],
  "rules": {
    "absolute_cap_semantics": "fail when accounted bytes are greater than or equal to 18 GB",
    "high_watermark_semantics": "all installs and builds stop at or above 85 percent until a reviewed configuration decision changes the budget",
    "model_downloads_in_bootstrap": false,
    "local_builds_are_sequential": true
  }
}
